# Lean requirements for Render - skips face_recognition/dlib to avoid OOM (8GB+)
# Uses OpenCV fallback in attendance_poc.py; still works for face detection
opencv-python-headless>=4.8.0
requests>=2.28.0
